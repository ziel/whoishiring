"use strict";

const expect = require("chai").expect

describe("tests", function() {
    it("should have some tests", function() {
      let todo = "Write some tests!"
      expect(todo).to.be.false()
    })
});
